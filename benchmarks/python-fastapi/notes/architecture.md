# Fixture notes

The checkout route represents a production regression whose failure message is
selected only when the FastAPI project dependency and nested runtime
requirements include remain present. The health route, second failing test,
constraints, package index option, and extra dependencies are unrelated noise.
